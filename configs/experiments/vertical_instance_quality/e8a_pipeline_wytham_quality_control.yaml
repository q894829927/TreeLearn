# E8a score-only Wytham control. Labels must remain identical before Oracle use.
default_args:
  - configs/experiments/point_transformer/_pipeline_base_residual_mlp_seed_common.yaml
  - configs/_modular/sample_generation.yaml
  - configs/_modular/model.yaml
  - configs/_modular/grouping.yaml
  - configs/_modular/dataset_test.yaml
  - configs/experiments/wytham/_pipeline_common.yaml

grouping:
  seed_confidence_filter_mode: 'top_ratio'
  seed_confidence_keep_ratio: 1.0
  max_cluster_seed_points: 1500000

pretrain: 'work_dirs/base_residual_mlp_frozen_s43/best_base_residual_xy.pth'
tile_generation: False

quality_filter:
  score_instances: True
  enabled: False
  mode: 'top_ratio'
  keep_ratio: 0.85
  threshold: 0.0
  checkpoint: 'logs/vertical_quality/e4_vertical_mlp/checkpoints/vertical_mlp_seed42.pth'
  num_layers: 8
  device: 'cuda'

save_cfg:
  results_dir: 'results_vertical_quality_e8a_control'
  save_instance_diagnostics: False