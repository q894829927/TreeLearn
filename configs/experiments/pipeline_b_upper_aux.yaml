# B: upper-anchor auxiliary supervision, but original base-anchor clustering.
default_args:
  - "configs/_modular/sample_generation.yaml"
  - "configs/_modular/model.yaml"
  - "configs/_modular/grouping.yaml"
  - "configs/_modular/dataset_test.yaml"
  - "configs/experiments/_pipeline_common.yaml"

model:
  spatial_shape: [500, 500, 1000]
  use_upper_anchor: True
  upper_offset_loss_weight: 1.0
  axis_loss_weight: 0.0
  offset_loss_type: 'l2'

grouping:
  upper_anchor_weight: 0.0
  axis_height_weight: 0.0

# B and C deliberately share checkpoint T1.
pretrain: 'work_dirs/t1_dual_no_axis/epoch_1000.pth'
tile_generation: False

save_cfg:
  results_dir: 'results_b_upper_aux'
